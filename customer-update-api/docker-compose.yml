services:
  postgres:
    image: postgres:15
    container_name: customer-update-postgres
    environment:
      POSTGRES_DB: customer_update_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"